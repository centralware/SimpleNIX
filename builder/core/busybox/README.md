# Linux : BUSYBOX

[Simple Linux](https://users.simplenix.com/forum) BUSYBOX EXTENSION

##build _version=1.2.3_# allows a specific version to be passed for compilation, otherwise
the most recent stable version is automatically detected and compiled.

packaged-suid:   Applications which require super user abilities

packaged-nosuid: Applications which do not require special privilages

packaged-nets:   Additional network based applications (httpd, ftpd, tftpd, etc.)

